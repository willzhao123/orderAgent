{
  "name": "backend-skill-chat",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22.6 <25"
  },
  "scripts": {
    "bundle:faq": "node --experimental-strip-types scripts/bundleFaq.ts",
    "chat": "node --env-file-if-exists=.env --experimental-strip-types src/chat.ts",
    "start": "node --experimental-strip-types src/server.ts",
    "test": "node --experimental-strip-types --test test/*.test.ts"
  },
  "dependencies": {
    "fastify": "^5.10.0",
    "pg": "^8.22.0",
    "yaml": "^2.9.0"
  },
  "devDependencies": {
    "@types/pg": "^8.20.0"
  }
}
